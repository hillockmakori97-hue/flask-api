Sql Alchemy- This is where we never write sql queries, instead we use predefined classes and our own classes, A class is a collection of variables nad functionsb but then in a class, a function is called a maethod and a variable is called a property

SQL Aclcje